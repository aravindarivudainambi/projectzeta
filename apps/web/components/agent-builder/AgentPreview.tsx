"use client";

/**
 * Renders a live preview of the agent configuration being authored.
 *
 * The final implementation should show schema-aware summaries, validation hints,
 * and versioning metadata as the builder evolves.
 */
export function AgentPreview() {
  return <section>Agent preview placeholder.</section>;
}
