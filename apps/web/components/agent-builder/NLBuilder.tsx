"use client";

/**
 * Renders the natural-language builder shell.
 *
 * This client component should eventually own prompt input, streaming feedback,
 * and high-level builder actions while delegating state orchestration to hooks.
 */
export function NLBuilder() {
  return <section>Natural language builder placeholder.</section>;
}
