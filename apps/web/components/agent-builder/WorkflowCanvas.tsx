"use client";

/**
 * Renders the visual workflow canvas shell.
 *
 * Replace this placeholder with a `reactflow` implementation once node types,
 * edge rules, and serialization contracts are finalized.
 */
export function WorkflowCanvas() {
  return <section>Workflow canvas placeholder.</section>;
}
