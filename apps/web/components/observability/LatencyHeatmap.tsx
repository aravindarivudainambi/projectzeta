/**
 * Renders the latency heatmap placeholder.
 *
 * Replace this with a visual latency distribution component backed by observability aggregates.
 */
export function LatencyHeatmap() {
  return <section>Latency heatmap placeholder.</section>;
}
