"use client";

/**
 * Renders a single step card placeholder.
 *
 * Replace this with a structured view for tool inputs, outputs, timing, and error state.
 */
export function StepCard() {
  return <article>Run step placeholder.</article>;
}
