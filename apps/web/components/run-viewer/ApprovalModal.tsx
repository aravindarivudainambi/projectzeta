"use client";

/**
 * Renders a human approval interruption placeholder.
 *
 * The final implementation should surface risk context, approval controls,
 * and audit-friendly decision metadata.
 */
export function ApprovalModal() {
  return <dialog open={false}>Approval placeholder.</dialog>;
}
