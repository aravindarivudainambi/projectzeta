/**
 * Renders the members management placeholder.
 *
 * Replace this with role assignment, invite, and audit capabilities once IAM flows exist.
 */
export default function MembersPage() {
  return <main>Members placeholder.</main>;
}
