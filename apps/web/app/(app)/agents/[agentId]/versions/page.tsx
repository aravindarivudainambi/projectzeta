/**
 * Renders the behavioral version history placeholder.
 *
 * Replace this with a server-rendered audit and rollback view once versioning endpoints exist.
 */
export default function AgentVersionsPage() {
  return <main>Agent versions placeholder.</main>;
}
