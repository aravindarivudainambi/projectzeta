/**
 * Renders the agent edit workspace placeholder.
 *
 * The real view should hydrate editable configuration state and surface validation errors inline.
 */
export default function EditAgentPage() {
  return <main>Edit agent placeholder.</main>;
}
