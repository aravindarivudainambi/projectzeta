/**
 * Renders the per-agent observability deep-dive placeholder.
 *
 * Replace this with a server component that scopes cost, latency, and failure data to one agent.
 */
export default function AgentObservabilityPage() {
  return <main>Agent observability placeholder.</main>;
}
