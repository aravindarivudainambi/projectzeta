/**
 * Renders the connectors management placeholder.
 *
 * The real implementation should present integration status, OAuth entry points,
 * and tool capability summaries.
 */
export default function ConnectorsPage() {
  return <main>Connectors placeholder.</main>;
}
