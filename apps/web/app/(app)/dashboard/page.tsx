import { BuilderWorkspace } from "@/components/builder/BuilderWorkspace";

/**
 * Renders the main agent builder workspace inside the authenticated dashboard.
 */
export default function DashboardPage() {
  return <BuilderWorkspace />;
}
