/**
 * Renders the dashboard overview placeholder.
 *
 * The eventual server component should summarize agents, recent runs, and usage metrics.
 */
export default function DashboardPage() {
  return <main>Dashboard placeholder.</main>;
}
