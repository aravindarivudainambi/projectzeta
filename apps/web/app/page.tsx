/**
 * Renders the root landing page for the scaffolded application.
 *
 * Replace this placeholder with real auth-aware routing once session handling is implemented.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-950 px-6 py-24 text-slate-50">
      <div className="rounded-2xl border border-emerald-400/30 bg-slate-900/80 px-10 py-8 text-center shadow-2xl shadow-emerald-950/40 backdrop-blur">
        <p className="text-sm font-medium uppercase tracking-[0.3em] text-emerald-300">
          Agent Builder
        </p>
      </div>
    </main>
  );
}
