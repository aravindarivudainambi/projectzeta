/**
 * Renders the OAuth callback route placeholder.
 *
 * Replace this with a server-side handler or client bridge once the auth provider is chosen.
 */
export default function CallbackPage() {
  return <main>Auth callback placeholder.</main>;
}
