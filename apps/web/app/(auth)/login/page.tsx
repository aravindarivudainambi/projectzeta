/**
 * Renders the login route placeholder.
 *
 * The final implementation should host credential-based and SSO sign-in affordances.
 */
export default function LoginPage() {
  return <main>Login placeholder.</main>;
}
