import type { NextConfig } from "next";

/**
 * Defines the Next.js configuration used by the frontend application.
 *
 * Keep this file intentionally small until deployment, observability,
 * and image optimization requirements are finalized.
 */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
