"use client";

/**
 * Manages approval modal state for long-running agent executions.
 *
 * Replace this placeholder with event-driven approval orchestration once the API exists.
 */
export function useApproval() {
  return {
    isOpen: false,
  };
}
