/**
 * Returns placeholder auth configuration data for the frontend layer.
 *
 * Replace this with real provider configuration and callbacks once identity decisions are final.
 */
export function getAuthConfig() {
  return {
    providers: [],
  };
}
