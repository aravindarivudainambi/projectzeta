use anyhow::Result;

/// Creates and waits on a human approval checkpoint for a sensitive action.
pub async fn request_approval() -> Result<()> {
    todo!("Persist approval requests and resume runs once a decision is recorded.")
}
