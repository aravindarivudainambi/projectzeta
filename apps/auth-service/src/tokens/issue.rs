use anyhow::Result;

/// Issues a short-lived token for a user or agent identity.
pub async fn issue_token() -> Result<()> {
    todo!("Implement claim assembly, signing, and token metadata persistence.")
}
