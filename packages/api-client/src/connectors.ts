/**
 * Lists available connectors for the active tenant.
 *
 * Replace this placeholder with a typed fetch helper once connector APIs are implemented.
 */
export async function listConnectors() {
  return [];
}

/**
 * Starts the connector authorization flow.
 *
 * The final helper should surface redirect metadata and any required PKCE state.
 */
export async function connectOAuth(_connectorName: string) {
  return null;
}
