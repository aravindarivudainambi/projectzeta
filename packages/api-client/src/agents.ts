/**
 * Lists agents visible to the current user.
 *
 * Replace this placeholder with a typed fetch helper that talks to the API gateway.
 */
export async function listAgents() {
  return [];
}

/**
 * Fetches a single agent by identifier.
 *
 * Keep the return type aligned with generated schema contracts once available.
 */
export async function getAgent(_agentId: string) {
  return null;
}
