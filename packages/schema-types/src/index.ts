/**
 * Represents a frontend-safe agent summary contract.
 *
 * Replace this placeholder with generated schema types once the Rust schemars pipeline exists.
 */
export interface AgentSummary {
  id: string;
  name: string;
  status: string;
}
